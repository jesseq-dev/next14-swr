import Elements from "@/src/components/Elements";

const Home = async () => {
  return (
    <main className="flex min-h-screen flex-col">
      <Elements />
    </main>
  );
};

export default Home;
